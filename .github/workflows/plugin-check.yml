name: Plugin Check

on:
  pull_request:
  push:
    branches:
      - 1.x
  workflow_dispatch:

jobs:
  plugin-check:
    uses: lubusIN/.github/.github/workflows/plugin-check.yml@main
    with:
      plugin-slug: blablablocks-slider-block
    secrets: inherit
